apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: root-cluster-baseline
  namespace: argocd
spec:
  project: default

  source:
    repoURL: git@github.com:Mariozdz/vault-tec.git
    targetRevision: main
    path: argocd/applications/lab/cluster-baseline

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true